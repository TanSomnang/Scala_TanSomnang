object DescendingOrder {
  def main(args: Array[String])
  {
   
      var a=Array[Int](3)
      var i=0
      
      for (i <- 0 until a.length)
      {
        print("Please, input all value: ")
        var a[i]=scala.io.StdIn.readLine();
      }
       
       print(a.sorted+" ");
}
}